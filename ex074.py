from random import randint
a = (randint(0, 100), randint(0, 100), randint(0, 100), randint(0, 100), randint(0, 100))
print(f'{a}\n'
      f'{max(a)}, {min(a)}')
# i can use 'max' to know the higher
# or use the 'min' to know the lower
